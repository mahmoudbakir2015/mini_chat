// buildscript {
//   dependencies {
//     // ... other dependencies
//  id 'com.google.gms.google-services' version '4.4.2' apply false
//   }
// }
// buildscript {
//     dependencies {
//         // ... other dependencies
//         classpath 'com.google.gms:google-services:4.3.15' // تأكد من إدخال هذا السطر
//     }
// }

// allprojects {
//     repositories {
//         google()
//         mavenCentral()
//     }
// }
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0' // تأكد من إدخال إصدار مناسب
        classpath 'com.google.gms:google-services:4.3.15'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
